# realty-admin-pwa
# 🏠 REAL ESTATE MANAGEMENT SYSTEM (REMS) v3.0
## Финальный паспорт проекта для новой ветки разработки

**Дата создания:** 18 июня 2026 
**Автор:** Эрнест Гареев (@ernest_812) 
**Статус:** Готов к разработке 
**Версия документа:** 3.0 (финальная архитектура)

---

## 📋 РЕЗЮМЕ ДЛЯ НОВОЙ ВЕТКИ

**Что строим:** Гибридную систему управления недвижимостью для риэлторов.

**Ключевое решение:** Отказ от Telegram-админки в пользу PWA + Google Sheets.

**Модель распространения:** Хаб (один GitHub аккаунт Эрнеста) + индивидуальные таблицы агентов.

**Стек:**
- **База данных:** Google Sheets (вместо Supabase)
- **Фото:** GitHub (вместо Cloudinary)
- **Админка:** PWA (Progressive Web App) для мобильных агентов
- **Клиентское приложение:** Telegram Mini App (уже работает)
- **Бэкенд-логика:** Google Apps Script (прокси между приложением и таблицей)

---

## 🎯 БИЗНЕС-МОДЕЛЬ И АРХИТЕКТУРА

### Модель "Хаб" (основной сценарий — 90% агентов)

```
┌─────────────────────────────────────────────────────────┐
│  GitHub аккаунт Эрнеста (777ernest888-oss)              │
│  ├── demo-miniapp-realty/ (клиентское приложение)       │
│  └── realty-admin-pwa/ (PWA-админка)                    │
└─────────────────────────────────────────────────────────┘
         │
         ▼
─────────────────────────────────────────────────────────┐
│  Агенты подключаются через agent_id                     │
│  ├── Агент №1 → agent_id=001 → своя Google Таблица      │
│  ├── Агент №2 → agent_id=002 → своя Google Таблица      │
│  └── Агент №3 → agent_id=003 → своя Google Таблица      │
└─────────────────────────────────────────────────────────┘
```

**Как это работает:**
1. Агент регистрируется через Google OAuth (1 клик)
2. Получает доступ к PWA-админке по ссылке: `https://777ernest888-oss.github.io/realty-admin-pwa/?agent=001`
3. При первом входе создаёт Google Таблицу по шаблону
4. Копирует ID таблицы в настройки PWA
5. Всё! Данные агента изолированы в его таблице.

**Премиум-сценарий (10% агентов — крупные агентства):**
- Отдельный GitHub аккаунт (white-label)
- Полная изоляция кода и данных
- Доплата за кастомизацию

---

## 📊 СТРУКТУРА GOOGLE ТАБЛИЦЫ (ПОЛНАЯ)

**Название листа:** `Properties`

| Колонка | Тип данных | Описание | Обязательно |
|---------|-----------|----------|-------------|
| **A: id** | Текст | Уникальный ID объекта (например, `spb-001`) | ✅ Да |
| **B: name** | Текст | Название ЖК (например, `ЖК "Аристократ"`) | ✅ Да |
| **C: district** | Текст | Район (например, `Василеостровский`) | ✅ Да |
| **D: metro** | Текст | Ближайшее метро (например, `Горный институт`) | ✅ Да |
| **E: price_from** | Число | Цена от (в млн руб, например, `14.6`) | ✅ Да |
| **F: price_to** | Число | Цена до (в млн руб, например, `75`) | ✅ Да |
| **G: rooms** | Текст | Количество комнат (например, `студия, 1, 2, 3`) | ✅ Да |
| **H: area_min** | Число | Площадь мин (м², например, `23.1`) | ✅ Да |
| **I: area_max** | Число | Площадь макс (м², например, `120.8`) | ✅ Да |
| **J: price_per_sqm** | Число | Цена за м² (например, `632035`) | ❌ Нет |
| **K: completion_soonest** | Текст | Ближайший срок сдачи (например, `II кв. 2029`) | ✅ Да |
| **L: status** | Текст | Статус (`Строится`, `Сдан`, `Частично сдан`) | ✅ Да |
| **M: completion_all** | Текст | Полный срок сдачи (например, `IV кв. 2030`) | ❌ Нет |
| **N: class** | Текст | Класс (`Комфорт`, `Бизнес`, `Премиум`) | ✅ Да |
| **O: finishing** | Текст | Отделка (`Без отделки`, `Предчистовая`, `Чистовая`) | ❌ Нет |
| **P: description** | Текст | Описание объекта | ❌ Нет |
| **Q: image_main** | URL | Ссылка на главное фото (GitHub) | ✅ Да |
| **R: images_gallery** | URL | Ссылки на галерею (через запятую) | ❌ Нет |
| **S: floor_plans_text** | Текст | Описание планировок | ❌ Нет |
| **T: floor_plans_images** | URL | Ссылки на планировки (через запятую) | ❌ Нет |
| **U: features** | Текст | Особенности (например, `Паркинг, Фитнес`) | ❌ Нет |
| **V: address** | Текст | Полный адрес (например, `Санкт-Петербург, ...`) | ✅ Да |
| **W: lat** | Число | Широта (например, `59.924528`) |  Нет |
| **X: lng** | Число | Долгота (например, `30.265167`) | ❌ Нет |
| **Y: active** | TRUE/FALSE | Активен ли объект (`TRUE`/`FALSE`) | ✅ Да |

### Дополнительные поля для хаба (добавить в таблицу):

| Колонка | Тип данных | Описание |
|---------|-----------|----------|
| **Z: owner_id** | Текст | ID агента (например, `001`) |
| **AA: created_at** | Дата | Дата создания объекта |
| **AB: updated_at** | Дата | Дата последнего обновления |

### Секретные поля (скрыты от клиентов через Apps Script):

| Колонка | Тип данных | Описание |
|---------|-----------|----------|
| **AC: owner_contacts** | Текст | Контакты собственника (ФИО, телефон) |
| **AD: commission** | Текст | Размер комиссии/условия |
| **AE: notes** | Текст | Заметки для себя (скрытые) |

---

## 🏗 АРХИТЕКТУРА СИСТЕМЫ

```
┌──────────────────────────────────────────────────────────────┐
│                    GOOGLE ECOSYSTEM (Бэкенд)                 │
│                                                              │
│  ┌─────────────────────┐      ┌───────────────────────────┐ │
│  │  Google Sheets      │◄────►│  Google Apps Script       │ │
│  │  (База данных       │      │  (API-прокси)             │ │
│  │   каждого агента)   │      │  • doGet() - чтение       │ │
│  │                     │      │  • doPost() - запись      │ │
│  └─────────────────────┘      └───────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
         ▲                                  ▲
         │ (Чтение/Запись)                  │ (Загрузка фото)
         │                                  │
┌─────────────────┐              ┌─────────┴────────────────┐
│  PWA АДМИНКА     │              │  КЛИЕНТСКОЕ ПРИЛОЖЕНИЕ   │
│  (Для агента)    │              │  (Telegram Mini App)     │
│                  │              │                          │
│ • Управление     │              │ • Просмотр каталога      │
│   объектами      │              │ • Фильтры и поиск        │
│ • Загрузка фото  │              │ • Связь с агентом        │
│   на GitHub      │              │                          │
│                  │              │ Открывается в Telegram   │
│ Открывается в    │              │ (Read-only режим)        │
│ браузере телефона│              └──────────────────────────┘
│ (Chrome/Safari)  │
│ Устанавливается  │
│ на экран         │
└──────────────────┘
         │
         ▼
┌─────────────────────┐
│    GITHUB           │
│ (Хранилище фото)    │
│ 777ernest888-oss    │
└─────────────────────┘
```

---

##  ТЕХНИЧЕСКИЕ ДЕТАЛИ

### 1. Google Apps Script (файл `Code.gs`)

**Функция `doGet(e)` — для клиентского приложения:**
```javascript
function doGet(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var data = sheet.getDataRange().getValues();
  var headers = data[0];
  var rows = data.slice(1);
 
  // Получаем agent_id из URL
  var agentId = e.parameter.agent_id || '';
 
  // Фильтруем по owner_id и active
  var ownerIdIndex = headers.indexOf('owner_id');
  var activeIndex = headers.indexOf('active');
 
  var filteredRows = rows.filter(function(row) {
    var matchesAgent = !agentId || (ownerIdIndex !== -1 && row[ownerIdIndex] === agentId);
    var isActive = activeIndex !== -1 ? row[activeIndex] === true || row[activeIndex] === 'TRUE' : true;
    return matchesAgent && isActive;
  });
 
  // Скрываем секретные колонки
  var secretColumns = ['owner_contacts', 'commission', 'notes'];
  var secretIndexes = secretColumns.map(function(colName) {
    return headers.indexOf(colName);
  }).filter(function(index) { return index !== -1; });
 
  var cleanData = filteredRows.map(function(row) {
    return row.map(function(cell, index) {
      if (secretIndexes.indexOf(index) !== -1) return '';
      return cell;
    });
  });
 
  return ContentService.createTextOutput(JSON.stringify([headers].concat(cleanData)))
    .setMimeType(ContentService.MimeType.JSON);
}
```

**Функция `doPost(e)` — для PWA-админки:**
```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var headers = sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
  var postData = JSON.parse(e.postData.contents);
  var action = postData.action; // 'create', 'update', 'delete'
 
  if (action === 'create') {
    var newRow = headers.map(function(header) {
      return postData.data[header] || '';
    });
    sheet.appendRow(newRow);
    return ContentService.createTextOutput(JSON.stringify({success: true}))
      .setMimeType(ContentService.MimeType.JSON);
  }
 
  // ... update и delete аналогично
}
```

### 2. PWA-админка (структура файлов)

```
realty-admin-pwa/
├── index.html          # Главный экран (список объектов)
├── form.html           # Форма создания/редактирования
├── settings.html       # Настройки (ввод ID таблицы)
├── style.css           # Стили
├── app.js              # Логика приложения
├── manifest.json       # PWA-манифест
└── icon-192.png        # Иконка приложения
```

### 3. Клиентское приложение (Telegram)

**Изменения в существующем коде `demo-miniapp-realty`:**
- Заменить Supabase на Google Sheets API
- Добавить параметр `?agent_id=...` в URL
- Сохранить всю текущую логику отображения

---

##  КАК ЭТО БУДЕТ ВЫГЛЯДИТЬ ДЛЯ АГЕНТА

### Первый запуск (1 раз):
1. Агент открывает ссылку: `https://777ernest888-oss.github.io/realty-admin-pwa/`
2. Видит экран: "Введите ID вашей Google Таблицы"
3. Создаёт таблицу по шаблону, копирует ID из URL
4. Вставляет ID, нажимает "Сохранить"
5. Нажимает "Добавить на главный экран"
6. Готово! Иконка появляется на рабочем столе.

### Ежедневная работа:
1. Агент нажимает на иконку "Моя Недвижимость"
2. Видит список своих объектов
3. Нажимает "+ Добавить объект"
4. Заполняет форму, выбирает фото из галереи
5. Нажимает "Сохранить"
6. Объект появляется в таблице и в клиентском приложении

---

## 🗺 ROADMAP (ПЛАН РАЗРАБОТКИ)

### Этап 1: Бэкенд (1-2 дня)
- [ ] Написать Apps Script (`doGet`, `doPost`)
- [ ] Настроить деплой Apps Script (Web App)
- [ ] Протестировать через Postman

### Этап 2: PWA-админка (3-4 дня)
- [ ] Создать репозиторий `realty-admin-pwa`
- [ ] Экран настроек (ввод ID таблицы)
- [ ] Список объектов (чтение из Sheets)
- [ ] Форма создания/редактирования
- [ ] Загрузка фото на GitHub
- [ ] manifest.json + иконки

### Этап 3: Адаптация клиентского приложения (1-2 дня)
- [ ] Заменить Supabase на Google Sheets в `demo-miniapp-realty`
- [ ] Добавить параметр `agent_id`
- [ ] Протестировать

### Этап 4: Тестирование и запуск (1-2 дня)
- [ ] Тест на iOS и Android
- [ ] Тест установки PWA на экран
- [ ] Написать инструкцию для агентов
- [ ] Запустить первого тестового агента

---

## 🔐 БЕЗОПАСНОСТЬ

### Что защищено:
- ✅ **Данные агентов изолированы** (каждый агент — своя таблица)
- ✅ **Секретные колонки скрыты** от клиентов (Apps Script фильтрует)
- ✅ **API-ключи спрятаны** в Apps Script (не в клиентском коде)
- ✅ **Фото хранятся на GitHub** (публичный доступ только к фото, не к таблице)

### Что НЕ защищено (и это нормально):
- ⚠️ Google Таблица доступна агенту (он владелец)
- ⚠️ GitHub-репозиторий с фото публичный (но это только фото, не данные)

---

## 📚 ИСТОРИЯ ПРИНЯТИЯ РЕШЕНИЙ

### Почему отказались от Supabase:
- ❌ Telegram WebView на мобильном не справлялся с Supabase SDK (зависания, таймауты)
- ❌ Сложность настройки для агентов (нужно создавать аккаунт, понимать SQL)
- ✅ Google Sheets проще, знакомее, бесплатнее

### Почему отказались от Cloudinary:
- ❌ Дополнительная настройка (аккаунт, API-ключи, подписи)
- ✅ GitHub уже работает, 1 ГБ хватит на 2000 фото
- ✅ Длинные URL не проблема (их никто не видит)

### Почему PWA вместо Telegram-админки:
- ❌ Telegram WebView нестабилен для тяжелых операций (загрузка фото, формы)
- ✅ PWA работает в обычном браузере (Chrome/Safari) — стабильно
- ✅ PWA устанавливается на экран как приложение

### Почему модель "Хаб":
- ✅ Один код — одно обновление (не нужно обновлять 100 копий)
- ✅ Агенты не заморачиваются с GitHub
- ✅ Вы контролируете версию

---

##  КОНТАКТЫ И РЕСУРСЫ

**Разработчик:** Эрнест Гареев 
**Telegram:** @ernest_812 
**GitHub:** 777ernest888-oss 
**Google Таблица (шаблон):** [ссылка будет добавлена] 
**Apps Script (деплой):** [URL будет добавлен после деплоя] 
**PWA-админка:** https://777ernest888-oss.github.io/realty-admin-pwa/ 
**Клиентское приложение:** https://777ernest888-oss.github.io/demo-miniapp-realty/

---

# 📋 ПАСПОРТ ПРОЕКТА: DEMO-MINIAPP-REALTY
**Дата актуализации:** 21.06.2026
**Владелец:** Эрнест
**Статус:** Фаза 1 (Backend) — В процессе внедрения

## 1. ЦЕЛЬ ПРОЕКТА
Создать экосистему из двух приложений для частного инвестора/агента по недвижимости:
1.  **Админка (Mobile-first):** Инструмент для управления базой объектов (ЖК и квартиры), загрузки фото, редактирования данных. Работает через Telegram Mini App.
2.  **Клиентское приложение:** Витрина для конечных покупателей/инвесторов с фильтрами, карточками объектов, галереями и формами захвата лидов.

**Ключевая ценность:** Полный отказ от внешних SaaS (Supabase) в пользу собственной инфраструктуры на Google Sheets + Apps Script для снижения затрат и полного контроля над данными.

## 2. ТЕХНИЧЕСКИЙ СТЕК И АРХИТЕКТУРА

### Backend (Единый источник правды)
-   **База данных:** Google Sheets (два основных листа: `Listings`, `Apartments`)
-   **API:** Google Apps Script (Web App)
-   **Хранилище файлов:** Google Drive (папка `Realty Images`)
-   **Формат фото URL:** `https://lh3.googleusercontent.com/d/{FILE_ID}` (решает проблему CORS в `<img>`)
-   **Безопасность:**
    -   Чтение (`doGet`): Публичное, без PIN
    -   Запись (`doPost`): Защищена PIN-кодом `123456`
    -   Секретные колонки фильтруются при чтении

### Frontend: Админка
-   **Хостинг:** GitHub Pages (`777ernest888-oss.github.io/demo-miniapp-realty/admin.html`)
-   **Стек:** Vanilla JS + HTML/CSS (без фреймворков)
-   **Особенности:** Mobile-first, два типа форм (ЖК/Квартира), авто-расчёт цены за м²

### Frontend: Клиентское приложение
-   **Хостинг:** GitHub Pages (`777ernest888-oss.github.io/demo-miniapp-realty/index.html`)
-   **Конфигурация:** `client-config.json` (содержит `scriptUrl`, брендинг, настройки)
-   **Данные:** Читает оба листа (`Listings` + `Apartments`) через единый API
-   **Заявки:** Пока отправляются в Supabase (этап миграции на Sheets запланирован позже)

## 3. СТРУКТУРА ДАННЫХ (GOOGLE SHEETS)

### Лист `Listings` (Жилые комплексы)
| Поле | Тип | Примечание |
|------|-----|-----------|
| id | string | Авто-UUID |
| name | string | «Название» (универсальное) |
| address | string | Обязательно |
| district, metro | string | Опционально |
| price_from, price_to | number | В миллионах ₽ |
| price_per_sqm | number | В рублях |
| area_min, area_max | number | Диапазон площадей |
| rooms | string | Комнатность |
| completion_soonest, completion_all | string | Два срока сдачи |
| class, finishing, status | string | Класс, отделка, статус стройки |
| description, features | string | Текст и преимущества |
| image_main | string | URL главного фото |
| images_gallery | string | URLs через запятую |
| floor_plans_images | string | URLs планировок |
| floor_plans_text | string | Текстовое описание планировок |
| lat, lng | number | Координаты |
| active | boolean | TRUE/FALSE/HIDDEN (по умолчанию TRUE) |
| created_at | string | ISO-дата, не изменяется |
| updated_at | string | ISO-дата, обновляется автоматически |

### Лист `Apartments` (Отдельные квартиры)
| Поле | Тип | Примечание |
|------|-----|-----------|
| id | string | Авто-UUID |
| name | string | «Название» (напр., «2-комнатная квартира») |
| address | string | Обязательно |
| district, metro | string | Опционально |
| market_type | string | `new` (строится) / `secondary` (готовая) |
| price | number | В миллионах ₽ |
| price_per_sqm | number | **Авто-расчёт:** `(price * 1M) / area_total` |
| area_total | number | Общая площадь (обязательно) |
| area_living, area_kitchen | number | Жилая и кухня (опционально) |
| rooms | string | Комнатность |
| floor, floors_total | number | Этаж и этажность |
| completion_date | string | Только если `market_type = new` |
| class, finishing | string | Класс и отделка |
| description, features | string | Текст и преимущества |
| image_main, images_gallery, floor_plans_images | string | Фото |
| lat, lng | number | Координаты |
| active | boolean | По умолчанию TRUE |
| created_at, updated_at | string | Авто-даты |

## 4. УТВЕРЖДЁННЫЕ БИЗНЕС-ПРАВИЛА

1.  **Цена:** Хранится в **миллионах** (для удобства восприятия). Цена за м² — в **рублях**. Связь автоматическая для квартир.
2.  **ID и даты:** Генерируются скриптом. Агент их не видит и не редактирует.
3.  **Active:** По умолчанию `TRUE`. При обновлении объекта, если поле не передано — сохраняется текущее значение. Поддержка трёх состояний: показывать / скрыть / удалить.
4.  **Фото:**
    -   Загрузка: Base64 → Apps Script → Google Drive
    -   URL: `lh3.googleusercontent.com/d/...` (не `drive.google.com/uc?id=...`)
    -   Удаление объекта: Каскадное удаление всех фото с Drive
    -   Добавление планировок: Merge (добавление к существующим, не замена)
    -   Input file: Без атрибута `capture` (открывается галерея, не камера)
5.  **Типы объектов:** При создании агент выбирает «ЖК» или «Квартира». От этого зависит набор полей.
6.  **Расширяемость:** Новые типы (Коммерция, Загородка) добавляются через новые листы с авто-созданием. Архитектура уже готова.

## 5. ИЗВЕСТНЕННЫЕ ПРОБЛЕМЫ И СТАТУС РЕШЕНИЯ

| # | Проблема | Статус | Решение |
|---|----------|--------|---------|
| 1 | handleUpdate затирает active | ✅ Решено | Merge полей в новом Code.gs |
| 2 | Камера вместо галереи | ⏳ Фаза 2 | Убрать `capture` в admin.html |
| 3 | Старый формат URL фото | ✅ Решено | Переход на lh3 + ручная правка старых |
| 4 | Удаление не чистит Drive | ✅ Решено | extractFileId + setTrashed в handleDelete |
| 5 | Загрузка фото 30 сек | ⏳ Фаза 2 | Убрать setTimeout в app.js админки |
| 6 | Планировки стираются | ⏳ Фаза 2 | Логика merge в admin.html |
| 7 | Предпросмотр фото при edit | ⏳ Фаза 2 | Подгрузка существующих URL в форму |
| 8 | Серые значения вместо плейсхолдеров | ⏳ Фаза 2 | Замена placeholder в admin.html |
| 9 | Лишние поля в предпросмотре списка | ⏳ Фаза 2 | Оставить только Фото/Название/Адрес |
| 10 | Заявки уходят в Supabase | ⏳ Будущее | Миграция на Sheets после стабилизации |

## 6. ТЕКУЩИЙ ЭТАП И СЛЕДУЮЩИЕ ШАГИ

### ✅ ЗАВЕРШЕНО:
- Архитектура утверждена
- Структура данных определена
- **Code.gs (Фаза 1)** написан и готов к деплою

### 🔄 ТЕКУЩАЯ ЗАДАЧА:
1. Эрнест заменяет Code.gs на новый код из паспорта
2. Запускает `testSetup()` для создания листов
3. Деплоит новую версию
4. Тестирует CRUD через Apps Script Console / Postman

### 📋 СЛЕДУЮЩИЙ ЭТАП (ФАЗА 2: ADMIN FRONTEND):
- Модальное окно выбора типа (ЖК/Квартира)
- Две динамические формы создания/редактирования
- Авто-расчёт price_per_sqm на клиенте
- Исправление input file (галерея)
- Merge планировок
- Упрощённый список объектов
- Плейсхолдеры

### 📋 БУДУЩИЙ ЭТАП (ФАЗА 3: CLIENT FRONTEND):
- Чтение из двух листов
- Единое форматирование цены
- Поддержка новых полей квартир
- Фильтрация по market_type

## 7. ПРАВИЛА ВЗАИМОДЕЙСТВИЯ С AI

1.  **ВСЕГДА присылать ПОЛНЫЙ файл кода.** Никаких сокращений, многоточий, «остальной код без изменений».
2.  **Не забегать вперёд.** Исходить только из предоставленных скриншотов и фактов. Не придумывать интерфейс/ошибки заранее.
3.  **Шаг за шагом.** Один вопрос — один ответ. Не выдавать маршрут из 10 шагов, если не пройдён первый.
4.  **Проактивность.** Предвидеть проблемы и предлагать обходные пути ДО того, как пользователь с ними столкнётся.
5.  **Безопасность.** Никогда не предлагать хранить секреты/PIN в открытом виде в клиентском коде.
6.  **Контекст.** При потере памяти — пользователь присылает этот паспорт. AI восстанавливает все нюансы и продолжает работу.

## 8. КЛЮЧЕВЫЕ ССЫЛКИ

-   **Репо:** `github.com/777ernest888-oss/demo-miniapp-realty`
-   **Админка:** `777ernest888-oss.github.io/demo-miniapp-realty/admin.html`
-   **Клиент:** `777ernest888-oss.github.io/demo-miniapp-realty/index.html`
-   **Apps Script URL:** `https://script.google.com/macros/s/AKfycby6kLvHmpgAvh_yS_ODVwgaQ8HVEugSZCuZgpf-vSrjxyjBljjfv40mQn3hywfKDGlc9w/exec`
-   **PIN:** `123456`

---

**ИНСТРУКЦИЯ ДЛЯ ВОССТАНОВЛЕНИЯ КОНТЕКСТА:**
При начале нового чата отправьте:
> «Восстанови контекст по паспорту проекта. [Вставьте весь текст паспорта выше]. Мы на этапе [укажите текущий этап]. Продолжаем.»

AI подтвердит понимание всех пунктов и продолжит работу без потери деталей.
